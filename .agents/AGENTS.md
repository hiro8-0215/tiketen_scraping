# カスタムルール（AIエージェントへの恒久的な指示）

以下のルールは、このプロジェクトにおいてAIがタスクを実行する際に必ず遵守すべきガイドラインです。

## コードのクリーンアップに関するルール
- 検証用、データ確認用、一時的なテストのために作成したスクリプト（例: `check_XXX.py`, `get_XXX.py` など）は、結果を確認し用が済んだら**必ずそのターンのうちに削除**し、プロジェクトフォルダ内を常に綺麗に保つこと。
- 人間から「いらないコードは消して」と言われなくても、自発的に削除を行うこと。
